# Major-language demos

各ファイルを Zed で開き、タグ名または `TAG(owner)` だけに背景が付き、コメント記号・コロン・本文が通常コメント色のままか確認する。

含まれる例:

- Clojure、JavaScript、TypeScript、TSX、Python、Rust、Go、Java、Kotlin
- C、C++、C#、Ruby、PHP、Swift、Dart、Scala、Zig
- Bash、Elixir、Lua、R、HTML、CSS、SQL、YAML、TOML、Dockerfile、Svelte
